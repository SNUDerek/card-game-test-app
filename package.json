{
  "name": "card-game-test-app",
  "private": true,
  "version": "0.1.0",
  "description": "Self-hosted multiplayer tabletop for prototyping physical card games.",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "typecheck": "npm run build -w shared && npm run typecheck -w server && npm run typecheck -w client",
    "test": "npm run build -w shared && npm run test -w server"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "typescript": "^5.9.3"
  }
}
